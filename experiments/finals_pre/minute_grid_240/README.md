# 240 位分钟输入修正：单种子配对实验

2026-09-06 已完成，结果已拉回，训练机已执行 shutdown，schedule 已停用。见 [结果与解释](../../../reports/dependencies/finals_pre/minute_grid_240/20260906/README.md)。240 位本次中性化 RankIC 为 0.02624，242 位为 0.02708；多空 Sharpe 分别为 6.363、6.298。单种子没有显示全面提升。

将 active M_raw 输入改为 09:31–11:30、13:01–15:00，共 240 个固定时刻。缺失的真实分钟仍占自己的位置，不把后面的记录向前挤。17 个通道的数值、有效性标记、模型结构与损失保持原样。

本次先训练 clock240，再训练 clock242。两者同为 seed 20260801、3 epoch、2019–2023 年历史、一个交易日标签隔离、2024 年 O2C 评价。每步一个交易日，最多 1200 只股票；D=96，核 3/15/60，3 个 TCN block，AdamW 4e-4，AMP。采用已有 Polars loader；每 50 步记录时间。训练最后保存 checkpoint，再推理 2024 年。四项评价沿用 E2 的行业与 Barra 中性化口径。

clock242 是修正前 c0dcd219 的**按时钟对齐版本**（空位在 0、121）。原始 submission 是另一个历史布局，尾部有两个空位；本次不把它与当前 242 位基线混称为同一实现。原始 submission、旧权重和旧成绩均保留。历史 E5 的 242 位兼容转换仍单独保留，它不参与本次 M_raw 训练。

`run_pair.py` 需要两个独立代码目录，分别保存修正前与修正后 src/fastpack；两个目录使用相同的 trainer，以保证进度记录与训练循环一致。输出 `pair_manifest.json`（源码/数据校验和及完整命令）、`status.json`、两个 checkpoint、OOS 因子、逐日中性化 RankIC、`comparison.csv/json` 和完整日志。必须核对 241 个评价日与相同股票日期键。

本地 schedule 轮询，完成或失败后先把产物拉回本地，再执行 **autodl-bigquant 远程机器**的 `shutdown`。关机不由训练脚本执行。一个种子只能判断这次改动的表现，不能证明普遍优劣；当前展示里的历史成绩不能自动改称 240 位结果。
