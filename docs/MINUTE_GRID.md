# 当前分钟输入：240 个固定时刻

当前研究代码统一使用上午 09:31–11:30、下午 13:01–15:00，共 240 个分钟收盘时刻。上午对应索引 0–119，下午对应 120–239。缺失分钟保留原位置，并由 mask 标记；后续记录不会向前移动。09:30、13:00 不是这份源数据的分钟收盘记录，不额外插入空位。

## 当前入口

| 范围 | 当前行为 |
|---|---|
| `src/alpha_models/microstructure.py`、`microstructure_v2.py` | 默认 240；pandas 与 Polars 打包使用固定时间戳位置 |
| E1、E2、E3、E5、E6 与 M_raw/M_l5 重训脚本 | 新训练使用 240；默认结果目录新增 `clock240`，避免跳过训练或读到旧结果 |
| E5 的 40 通道存储 | 可读原 242 位导出或新 240 位导出；旧数据只删除经检查为空的两个预留位置，最终送入模型的是 240 位 |
| v2 checkpoint 复用 | 请求配置必须与 checkpoint 配置相同，长度不一致直接报错 |
| M_raw、M_l5 提交生成器 | 新导出要求真实的 240 位 checkpoint；清单长度取自权重配置，不将旧权重重新标记为 240 |
| `work/m_multiaxis`、开发 notebook 和生成器 | 当前默认长度 240 |
| 冻结权重重放、E4 | 使用提交包内的历史模型和打包器，长度读取该权重自己的配置 |

E1/E2 的新结果位于各实验根目录下的 `clock240/`；E3/E6 位于 `o2c/clock240/`；E5 位于 `o2c_autodl/clock240/`。对应 `run.sh` 与 `score.py` 的默认路径一起更新。使用自定义输出路径时，也应与旧实验目录分开。

M_raw 重新导出时，运行 `python scripts/build_unified_m_raw_submission.py --help`，显式提供 checkpoint、SHA-256、训练 commit、训练区间、block 与 protocol。默认从当前 `src/alpha_models` 打包到 `submissions/m_raw_240/`。M_l5 默认读取 `reports/dependencies/m_v3_final/clock240/`，导出到 `submissions/m_l5_240/`，并校对 checkpoint 与 manifest 的完整配置。

本次代码修改没有生成新的正式提交包，也没有替换平台上的权重。新包必须在实际训练和平台推理核验后再发布。

## 仍然保留 242 的位置

- 已提交的冻结 bundle、权重配置、历史训练日志与评价结果：保留其真实输入定义及校验和。
- `minute_grid_240/` 中的配对对照：242 是被比较的历史配置。
- E5 旧存储兼容代码和拒绝错误长度的测试：需要明确识别历史长度。
- “2024 年 242 个交易日”和 “242000 行因子”：它们是日期数与结果行数，不是分钟长度。

原 submission 的布局是 240 条记录后补两位；修正前研究代码是 242 个固定时钟位置，在索引 0、121 留空。两者不同。当前配对实验比较的是固定时钟 240 与 242，没有重跑原 submission。重新复现历史成绩要检出原 commit，使用对应权重、源代码和配置。

## 已有核验与性能边界

三个 seed 的配对已经完成，详见[原始汇总与运行记录](../reports/dependencies/finals_pre/minute_grid_240/additional_seeds_20260906/README.md)。240 位平均中性化 RankIC 为 0.028562，242 位为 0.028406；Sharpe 三个配对均更高，ICIR 均值略低。未进行显著性或等效性检验，不能将这项输入修正写成已证明普遍提升性能。

代码测试覆盖固定时刻和午休边界、缺失分钟不移位、pandas/Polars 一致性、旧存储转换不丢观测、旧 checkpoint 配置拒绝、当前源码导出与加载后预测一致性。历史 bundle 的校验和测试同时保留，用来确认这次修改没有改动已提交模型。
