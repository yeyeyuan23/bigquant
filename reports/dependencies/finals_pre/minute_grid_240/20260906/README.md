# 240 位分钟输入：单种子对照结果

两组训练与评价已完成。240 位本次的 RankIC、ICIR 和压力日 ICIR 略低，多空 Sharpe 略高。这组结果没有显示全面提升，也不足以判定两种长度的普遍优劣。240 位修正的依据是匹配实际分钟收盘时刻，不能把输入修正表述成已验证的收益提升。

| 指标 | 240 位 | 242 位 | 240 − 242 |
|---|---:|---:|---:|
| 中性化 RankIC | 0.026237 | 0.027079 | -0.000842 |
| RankIC IR | 0.599047 | 0.629232 | -0.030186 |
| 五分位多空 Sharpe | 6.363302 | 6.298222 | +0.065080 |
| 压力日 ICIR | 0.308611 | 0.341959 | -0.033348 |

两组均为 seed 20260801、3 epoch。训练期 2019-01-02 至 2023-12-28，共 1213 日；保留一个交易日标签隔离；评价为 2024 年 241 个有效 O2C 因子日。主表全部沿用 E2 的行业与 Barra 中性化口径。`oos_metrics.json` 中的 `rank_ic_mean` 是未中性化诊断，不与本表混用。

对照是修正前 src 的固定时钟 242 位布局，空位在 0、121；原 submission 的末尾补两个空位是另一种历史布局。本次没有重跑原 submission，也没有重新核验所有历史消融和正式成绩。

同一初始化权重、三轮日期及抽样顺序、三天真实输入特征与 mask 的校验和全部匹配。配置除 max_minutes 外一致，参数量都为 217953；两个已保存 checkpoint 均为从零训练，241 个评价日均有预测。完整因子路由含 242000 行，两组均有 1465 行按既有规则补中性值；实际评分只取有下一交易日标签的 241 日。

运行于 RTX 4090 D、16 CPU / 80 GiB 配额，线程上限 8。240 位训练加推理 1770.6 秒；242 位 1757.7 秒；含最终评分合计 3535.1 秒（58 分 55 秒）。这次测得的时间差很小，不支持提速结论。

2026-09-06 00:50:22 启动，01:49:17 完成（北京时间）。02:00:06 拉回并核验 28 个远程文件后执行 shutdown；后续 SSH 连接关闭，无法建立 shell。本地 schedule 240 已停用。未把 SSH 状态表述为平台计费审计。

结果证据：`comparison.csv/json`、两组 `daily_neutral_rank_ic.csv`、`pair_manifest.json`、`local_validation.json`、`git_source_audit.json`、`collection_receipt.json`、`shutdown_verification.json` 与训练日志。37 项相关测试通过，测试记录见 `validation.log`。两个 checkpoint、全量 parquet 与执行时源码快照保存在本地该目录，未提交大文件。

已核对实际执行源码：240 位模型/packer 对应 Git 232563c，242 位模型/packer 对应 c0dcd21，两个训练器均对应 232563c。对应文件 SHA-256 已与这些 Git 对象逐项比较。
